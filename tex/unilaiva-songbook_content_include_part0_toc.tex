%% unilaiva-songbook_content_include_part0_toc.tex
%% ===============================================
%%
%% This file is a partial .tex file to be included with \input macro
%% to any Unilaiva songbook main .tex file within \begin{document}
%% and \end{document} like this:
%%
%%   \input{tex/unilaiva-songbook_content_include_part0_toc.tex}
%%
%% This file contains the TOC.
%%

  % to have totally flat right edge of TOC (microtype package):
  \microtypesetup{protrusion=false} % To have a flat right side in TOC
  \tableofcontents
  \microtypesetup{protrusion=true} % back to default (microtype package)

  % Removes the overlapping page number in the bottom of the first TOC page:
  \addtocontents{toc}{\protect\thispagestyle{empty}}
  % Adds text "Page" on top of the TOC:
  \addtocontents{toc}{~\hfill\textbf{Page}\par}
